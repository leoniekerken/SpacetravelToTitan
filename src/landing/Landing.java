package landing;

/**
 * This class uses the controllers to perform a landing on Titan
 *
 */
public class Landing {

    //DISTURBANCES: mass, density and wind
    //Data that affects the landing of the probe on Titan
    final double mass = 6000.0; // mass of the landing module
    final double density = 4.0; // density Simulation has uniform and 4.4x as strong as on earth we said that it
    // is somewhere as the airliners are flying (15-17km) -> 0.909 -> resulting in a density of 4.
}
